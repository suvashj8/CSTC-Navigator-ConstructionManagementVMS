export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>Navigator VMS API</h1>
      <p>Next.js API gateway — use <code>/api/v1/*</code> from the frontend or mobile app.</p>
    </main>
  );
}
