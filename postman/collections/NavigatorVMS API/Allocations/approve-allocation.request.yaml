$kind: http-request
name: Approve Allocation
method: PUT
url: '{{baseUrl}}/api/v1/allocations/{{allocationId}}/approve'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{tenantToken}}'
headers:
  - key: X-Tenant-Subdomain
    value: '{{tenantSubdomain}}'
