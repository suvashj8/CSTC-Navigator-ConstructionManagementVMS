$kind: http-request
name: Release Allocation
method: PUT
url: '{{baseUrl}}/api/v1/allocations/{{allocationId}}/release'
order: 6000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{tenantToken}}'
headers:
  - key: X-Tenant-Subdomain
    value: '{{tenantSubdomain}}'
