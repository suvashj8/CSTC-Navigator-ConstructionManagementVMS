$kind: http-request
name: Receive Allocation
method: PUT
url: '{{baseUrl}}/api/v1/allocations/{{allocationId}}/receive'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{tenantToken}}'
headers:
  - key: X-Tenant-Subdomain
    value: '{{tenantSubdomain}}'
