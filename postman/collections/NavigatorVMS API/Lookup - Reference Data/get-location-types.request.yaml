$kind: http-request
name: Get Location Types
method: GET
url: '{{baseUrl}}/api/v1/location-types'
order: 15000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{tenantToken}}'
headers:
  - key: X-Tenant-Subdomain
    value: '{{tenantSubdomain}}'
