$kind: http-request
name: Get Ownership Types
method: GET
url: '{{baseUrl}}/api/v1/ownership-types'
order: 9000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{tenantToken}}'
headers:
  - key: X-Tenant-Subdomain
    value: '{{tenantSubdomain}}'
